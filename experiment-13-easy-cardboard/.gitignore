# Asset comercial Easy Cardboard 3.1 — licensiado, nao versionar
assets/*.blend

# Output pesado (esta no GCS / pode ser regenerado pelo pipeline)
output/*.blend
output/*.blend1
output/*.glb
output/cardboard_color.png
output/cardboard_normal.png
output/cardboard_roughness.png

# GLB na pasta web tambem (esta no GCS)
web/*.glb

# Screenshots de validacao (mantemos apenas os que vao no doc)
output/demo-shot.png
