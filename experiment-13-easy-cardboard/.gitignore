# Asset comercial Easy Cardboard 3.1 — licensiado, nao versionar
assets/*.blend

# Output (renders/bakes/blends — regenerar via pipeline)
output/

# GLBs em web/ (estao no GCS)
web/*.glb

# PNGs em web2/ tambem (estao no GCS, regenerar via pipeline)
web2/*/*.png
